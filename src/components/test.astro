---
import { db, snippet } from "astro:db";

export const snippets = await db.select().from(snippet);
console.log(snippets);
---

<div>
  <div>hello</div>
  {snippets && snippets.map((item) => <div>{item.title}</div>)}
</div>

<style>
  div {
    color: red;
  }
</style>
