---
title: Pinchflat
description: "Pinchflat is a self-hosted app for downloading YouTube content built using yt-dlp. It's designed to be lightweight, self-contained and easy to use."
references:
  - href: https://github.com/kieraneglin/pinchflat
    label: GitHub repo
---

import OpenWebUI from "@/components/guides/open-webui.astro";
import ReferenceLinks from "@/components/guides/reference-links.astro";
import { Code } from "@astrojs/starlight/components";
import yaml from "/src/files/compose/pinchflat.yml?raw";
import ytdlpBaseConfig from "/src/files/config/pinchflat/base-config.txt?raw";

<p>{frontmatter.description}</p>

### References

<ReferenceLinks references={frontmatter.references} />

---

## Make directory

```sh wrap preserveIndent
mkdir -p {{DOCKER_PATH_VAR}}/pinchflat/config/extras/yt-dlp-configs \
&& mkdir -p {{DOCKER_PATH_VAR}}/pinchflat/downloads \
&& cd {{DOCKER_PATH_VAR}}/pinchflat
```

## compose.yml

```sh
nano compose.yml
```

<Code lang="yml" title="compose.yml" wrap preserveIndent code={yaml} />

## base-config.txt

```sh
nano ./config/extras/yt-dlp-configs/base-config.txt
```

<Code lang="sh" title="base-config.txt" wrap preserveIndent code={ytdlpBaseConfig} />

## Start container

```sh
docker compose up -d
```

## Open web ui

<OpenWebUI port="8945" />
