---
title: Arcane
description: "A beautiful, intuitive interface for managing your Docker containers, images, networks and volumes."
references:
  - href: https://github.com/getarcaneapp/arcane
    label: GitHub repo
  - href: https://getarcane.app/docs
    label: Docs
---

import OpenWebUI from "@/components/guides/open-webui.astro";
import ReferenceLinks from "@/components/guides/reference-links.astro";
import { Code } from "@astrojs/starlight/components";
import yaml from "/src/files/compose/arcane.yml?raw";
import env from "/src/files/env/arcane.env?raw";

<p>{frontmatter.description}</p>

### References

<ReferenceLinks references={frontmatter.references} />

---

## Make directory

```sh wrap preserveIndent
mkdir -p {{DOCKER_PATH_VAR}}/arcane && cd {{DOCKER_PATH_VAR}}/arcane
```

## compose.yml

```sh
nano compose.yml
```

<Code lang="yml" title="compose.yml" wrap preserveIndent code={yaml} />

## .env

```sh
nano .env
```

<Code lang="dotenv" title=".env" wrap preserveIndent code={env} />

## Start container

```sh
docker compose up -d
```

## Open web ui

<OpenWebUI port="3552" />
