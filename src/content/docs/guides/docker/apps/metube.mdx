---
title: MeTube
description: "Web GUI for youtube-dl (using the yt-dlp fork) with playlist support. Allows you to download videos from YouTube and dozens of other sites."
references:
  - href: https://github.com/alexta69/metube
    label: GitHub repo
---

import OpenWebUI from "@/components/guides/open-webui.astro";
import ReferenceLinks from "@/components/guides/reference-links.astro";
import { Code } from "@astrojs/starlight/components";
import yaml from "/src/files/compose/metube.yml?raw";
import nfoScript from "/src/files/config/metube/nfo-script.sh?raw";
import options from "/src/files/config/metube/options.json?raw";

<p>{frontmatter.description}</p>

### References

<ReferenceLinks references={frontmatter.references} />

---

## Make directory

```sh wrap preserveIndent
mkdir -p {{DOCKER_PATH_VAR}}/metube && cd {{DOCKER_PATH_VAR}}/metube
```

## docker-compose.yml

```sh
nano docker-compose.yml
```

<Code lang="yml" title="docker-compose.yml" wrap preserveIndent code={yaml} />

## Setup config files

```sh
mkdir config && nano ./config/options.json
```

<Code lang="json" title="options.json" wrap preserveIndent code={options} />

```sh
nano ./config/nfo-script.sh
```

<Code lang="sh" title="nfo-script.sh" wrap preserveIndent code={nfoScript} />

```sh
chmod +x ./config/nfo-script.sh
```

## Start container

```sh
docker compose up -d
```

## Open web ui

<OpenWebUI port="8081" />
