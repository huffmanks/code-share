---
title: Paperless-ngx
description: "Paperless-ngx is a document management system that transforms your physical documents into a searchable online archive so you can keep, well, less paper."
references:
  - href: https://github.com/paperless-ngx/paperless-ngx
    label: GitHub repo
  - href: https://docs.paperless-ngx.com/
    label: Docs
---

import { Code } from "@astrojs/starlight/components";
import yaml from "/src/files/compose/paperless-ngx.yml?raw";

<p>{frontmatter.description}</p>

### References

<ul>
  {frontmatter.references.map((ref) => (
    <li>
      <a href={ref.href} target="_blank" rel="noopener">
        {ref.label}
      </a>
    </li>
  ))}
</ul>

## Create/change directory for Paperless-ngx

```sh
mkdir -p {{{DOCKER_PATH_VAR}}}/paperless-ngx && cd {{{DOCKER_PATH_VAR}}}/paperless-ngx
```

## Copy docker-compose.yml

<Code lang="yml" title="docker-compose.yml" code={yaml} />

## Create docker-compose.yml

```sh
nano docker-compose.yml
```

## Start container

```sh
docker compose up -d
```

## Open web ui

{(<a href="http://localhost:8001" target="_blank" rel="noopener">http://localhost:8001</a>)}
