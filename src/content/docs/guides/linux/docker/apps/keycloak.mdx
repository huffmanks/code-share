---
title: Keycloak
description: "Keycloak is an open-source identity and access management solution. It makes it easy to add authentication to applications and secure services. Keycloak offers user federation, strong authentication, user management, fine-grained authorization and more."
references:
  - href: https://github.com/keycloak/keycloak
    label: GitHub repo
  - href: https://www.keycloak.org/documentation
    label: Docs
---

import OpenWebUI from "@/components/guides/open-webui.astro";
import ReferenceLinks from "@/components/guides/reference-links.astro";
import { Code } from "@astrojs/starlight/components";
import yaml from "/src/files/compose/keycloak.yml?raw";

<p>{frontmatter.description}</p>

### References

<ReferenceLinks references={frontmatter.references} />

## Create/change directory for Keycloak

```sh
mkdir -p {{{DOCKER_PATH_VAR}}}/keycloak && cd {{{DOCKER_PATH_VAR}}}/keycloak
```

## Create docker-compose.yml

```sh
nano docker-compose.yml
```

## Copy docker-compose.yml

<Code lang="yml" title="docker-compose.yml" code={yaml} />

## Start container

```sh
docker compose up -d
```

## Open web ui

<OpenWebUI port="8082" />
