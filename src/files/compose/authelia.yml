services:
  authelia:
    image: authelia/authelia
    container_name: authelia
    volumes:
      - ./authelia:/config
    networks:
      - dns-proxy-network
    restart: unless-stopped
    environment:
      - TZ={{TIME_ZONE_VAR}}

  redis:
    image: redis:alpine
    container_name: authelia-redis
    networks:
      - dns-proxy-network
    restart: unless-stopped
